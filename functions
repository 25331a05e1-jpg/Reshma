#include<stdio.h>
int fun(int,int); //declaration
	int main()
	{
	int a,b,result;
	printf("enter two numbers:");
	scanf("%d %d",&a,&b);
	result=fun(a,b);
	return 0;
	}
	
	int fun(int a,int b)
	{
	return a+b;
	}
